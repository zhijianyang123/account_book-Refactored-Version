本文档面向 AI 编程助手。请根据以下需求，使用 Flutter 3.x 生成完整的 Android 应用代码。所有 UI 必须采用 iOS 风格，并基于 liquid\_glass\_widgets 包实现液态玻璃效果。不要使用任何其他玻璃效果库，不要编写与需求无关的代码。

一、项目概述
项目名：记账本

类型：Flutter 本地离线收支记录与统计应用

目标平台：Android

技术栈：Flutter 3.x + Provider + sqflite + fl\_chart + intl + liquid\_glass\_widgets

数据存储：本地 SQLite，单账本，不设计多账本

核心目标：快速记一笔、看清钱去哪了、能导入导出 JSON

默认分类：餐饮、交通、购物、居家、娱乐、医疗

二、设计风格与视觉要求
整体风格：参考 iOS 原生设计，使用 Cupertino 组件体系，整体呈现简洁、圆润、通透的质感。

液态玻璃效果：所有卡片、按钮、底部导航、悬浮按钮、弹窗等，均使用 liquid\_glass\_widgets 包提供的液态玻璃组件实现。请严格参考该包官方示例页面中的用法，不要自行发明其他玻璃效果。

背景：每个页面使用渐变背景，深色模式与浅色模式使用不同的渐变色组，切换主题时背景平滑过渡。

避免白屏闪烁：应用启动时需正确初始化液态玻璃效果，确保首帧不出现白色闪烁。

全局布局：每个页面使用该包提供的玻璃脚手架作为根布局，自动处理背景、状态栏和层级关系。

字体与图标：使用 iOS 风格字体和 Cupertino 图标，金额、日期等数字使用等宽字体或清晰易读的排版。

交互反馈：按钮点击、列表滑动、删除等操作需有流畅的动画和触感反馈（如适用）。

三、信息架构
底部导航（三个 Tab）
明细：默认首页，看账、找账、改账

统计：看趋势、看占比

我的：管理账户、分类、标签、预算、数据、设置

全局入口
记一笔悬浮按钮：位于右下角、底部导航上方，不遮挡列表内容。仅在明细、统计、我的三个 Tab 显示；设置页、导入预览页等独立页面隐藏此按钮。

设置入口：我的 → 应用设置

数据导入导出入口：我的 → 数据管理

独立页面
记一笔 / 编辑账单

搜索与筛选

分类管理

账户管理

标签管理

商家管理

预算管理

数据管理

导入预览

导出选项

应用设置

关于

四、三个 Tab 功能需求
4.1 明细 Tab
定位：最高频页面，快速查看和操作账单。

顶部区域：

月份切换：左右箭头 + 点击年月可弹出选择器

本月概览：收入、支出、结余三个数字，使用玻璃卡片展示

搜索图标、筛选图标

列表区域：

按日期分组展示当月账单

每个日期分组显示当天的收入、支出、结余小计

每条账单显示：分类图标、分类名、备注、账户、金额（支出红色、收入绿色）

点击某条账单进入编辑页面

左滑删除，删除后提供撤销提示

长按进入多选模式，支持批量删除、批量修改分类

空状态时显示引导文案和“记一笔”按钮

筛选与搜索：

类型：全部、支出、收入、转账、退款

分类、账户、标签、金额范围、日期范围

支持组合筛选

支持保存常用筛选条件，保存后可在筛选面板快速调用

可选增强：

日历视图：某天有账单时显示圆点，点击日期可跳转对应账单

悬浮按钮：

右下角“+”进入记一笔页面

记一笔页面：

顶部切换：支出 / 收入 / 转账 / 退款

大金额键盘，支持快捷计算（如输入 12+8 自动得出 20）

分类九宫格选择

账户、日期、时间、备注、标签、商家、报销状态（未报销 / 已报销 / 部分报销）

底部按钮：保存、保存并继续

编辑时同一页面进入，可删除该笔账单

4.2 统计 Tab
定位：看趋势、看占比，不直接管理数据。

顶部区域：

周 / 月 / 年切换

月份 / 年份选择

支出 / 收入切换

图表区域：

饼图：仅展示支出分类占比，收入不计入饼图

折线图 / 柱状图：收入、支出趋势

分类汇总列表 + 进度条

预算执行卡片：显示预算总额、已用金额、剩余额度、每日可用额度，超支时文字变红（仅 UI 颜色标记，不触发系统通知）

账户资产趋势：折线图展示账户总资产变化

标签统计、商家统计：列表或横向柱状图

交互：

点击饼图扇区，高亮对应分类

点击分类，跳转到明细 Tab 并自动筛选该分类

支持导出统计图片

统计范围严格遵循左闭右开 \[start, end)

4.3 我的 Tab
定位：管理中枢。

顶部卡片：

本月支出、收入、结余

预算剩余

账户总资产

右上角齿轮图标：进入应用设置

分组列表：

资产

账户管理：现金、微信、支付宝、银行卡、信用卡、投资账户

账户余额、初始余额、是否计入总资产

隐藏账户、停用账户、排序

转账记录

记账资料

分类管理：默认分类、自定义分类、图标、颜色、排序、二级分类

删除分类后，关联账单的 category\_id 设为 NULL，回退到“无类型”

标签管理

商家管理

计划

预算管理：月预算、年预算、分类预算

进度条、剩余额度、每日可用额度

超支状态标记：仅 UI 颜色，不触发系统通知或提醒

可配置转账、退款是否计入预算

数据管理

导出 JSON

导入 JSON

清除所有数据，需二次确认

设置与关于

应用设置

关于：版本号、开源许可、意见反馈

五、独立页面详细需求
5.1 记一笔 / 编辑账单
使用玻璃脚手架，隐藏悬浮按钮

顶部为标题和关闭/删除按钮

类型切换、金额键盘、分类九宫格、账户选择、日期时间选择、备注输入、标签选择、商家选择、报销状态选择

保存后返回上一页，若开启“保存后继续记下一笔”，则清空表单继续留在当前页

编辑模式下回填已有数据，支持删除

5.2 搜索与筛选
顶部搜索框，支持关键词搜索备注、分类、账户、标签、商家

下方筛选面板：类型、分类、账户、标签、金额范围、日期范围

支持组合筛选，筛选结果实时更新

支持保存常用筛选条件，以列表形式展示，点击直接应用

5.3 分类管理
支出 / 收入分类分开管理

列表展示分类，支持拖拽排序

每个分类可编辑名称、图标、颜色、是否隐藏

支持二级分类

新增分类按钮

删除分类时弹出确认框，提示关联账单将回退到“无类型”

5.4 账户管理
列表展示所有账户，显示名称、类型、余额、是否计入总资产

支持新增、编辑、隐藏、停用、排序

账户余额随记账自动更新

转账时选择转出账户和转入账户

5.5 标签管理
列表展示所有标签，支持新增、编辑、删除

标签包含名称和颜色

5.6 商家管理
列表展示所有商家，支持新增、编辑、删除

5.7 预算管理
支持月预算、年预算、分类预算

显示进度条、剩余额度、每日可用额度

超支时进度条变红，文字变红，仅 UI 标记

可配置转账、退款是否计入预算

5.8 数据管理
导出 JSON：选择导出范围（全部数据 / 按日期范围 / 按月份 / 按账户 / 按分类），是否包含已删除数据

导入 JSON：选择文件 → 解析 → 校验格式和版本 → 显示导入预览 → 选择导入模式 → 执行导入 → 显示导入结果

清除所有数据：二次确认后清空

5.9 导出选项
选择导出范围、是否包含已删除数据

文件名格式：记账本备份\_YYYYMMDD\_HHmmss.json

支持保存本地、分享到其他应用

5.10 导入预览
显示分类、账户、交易、标签、预算数量

显示日期范围、冲突数量、重复交易数量

显示 schema 版本、应用版本

选择导入模式：覆盖、合并、追加、仅导入指定日期范围

冲突处理选项：分类同名（跳过/合并/新建）、账户同名（跳过/合并/新建）、交易重复（按 id 去重/按业务指纹去重/全部保留）、预算冲突（覆盖/保留两者/跳过）、标签同名（合并/新建）

业务指纹：日期 + 金额 + 类型 + 分类 + 账户 + 备注

版本兼容：低版本自动迁移，高版本提示先升级应用，字段缺失给默认值，未知字段忽略

异常处理：文件不是 JSON 或损坏时提示错误；金额、日期非法时跳过并记录；分类循环引用时断开并记录；账户不存在时创建“未知账户”或跳过

导入过程使用事务，失败自动回滚，不破坏原数据

导入完成后刷新明细、统计、预算、账户

导入结果可导出报告

5.11 应用设置
通用

主题：浅色、深色、跟随系统

语言

货币单位，默认人民币

一周第一天

默认记账类型：支出 / 收入

默认账户

记账偏好

保存后继续记下一笔

金额快捷计算

自动补零

是否显示退款、转账

支出 / 收入颜色方案

关于

版本号

开源许可

意见反馈

明确不放在设置：通知、隐私与安全、数据备份、多账本、提醒。

六、数据模型（概念描述）
单账本，不设计 books 表。所有数据属于同一个账本。

categories：id、name、type（expense/income）、icon、color、sort\_order、parent\_id、is\_default、is\_hidden

accounts：id、name、type、balance\_cents、initial\_balance\_cents、currency、icon、color、include\_in\_net\_worth、is\_hidden、sort\_order

transactions：id、type（expense/income/transfer/refund）、amount\_cents、category\_id、account\_id、to\_account\_id、date、time、note、merchant、reimbursement\_status、created\_at、updated\_at、deleted\_at

tags：id、name、color

transaction\_tags：transaction\_id、tag\_id

budgets：id、period\_type（month/year/custom）、amount\_cents、category\_id、start\_date、end\_date、include\_transfer

settings：key、value

金额以“分”为单位存储，显示时除以 100。

七、JSON 导入导出规范
导出 JSON
入口：我的 → 数据管理 → 导出 JSON

导出范围：全部数据、按日期范围、按月份、按账户、按分类

是否包含已删除数据

导出内容：schema\_version、app\_version、exported\_at、export\_type、categories、accounts、transactions、tags、transaction\_tags、budgets、settings

文件名：记账本备份\_YYYYMMDD\_HHmmss.json

支持保存本地、分享到微信、网盘、邮件

明确不包含：books 多账本、通知配置、隐私与安全配置、数据备份配置、提醒配置

导入 JSON
入口：我的 → 数据管理 → 导入 JSON

流程：选择 JSON 文件 → 解析 → 校验格式和版本 → 显示导入预览 → 选择导入模式 → 执行导入 → 显示导入结果

导入模式：覆盖、合并、追加、仅导入指定日期范围

冲突处理：分类同名、账户同名、交易重复、预算冲突、标签同名

业务指纹：日期 + 金额 + 类型 + 分类 + 账户 + 备注

版本兼容：低版本自动迁移，高版本提示先升级应用，字段缺失给默认值，未知字段忽略

异常处理：文件不是 JSON 或损坏时提示错误；金额、日期非法时跳过并记录；分类循环引用时断开并记录；账户不存在时创建“未知账户”或跳过

安全要求：导入过程使用事务，导入失败可回滚，导入完成后刷新明细、统计、预算、账户，导入结果可导出报告

八、验收标准
底部只有三个 Tab：明细、统计、我的，明细默认

不再有底部“设置”Tab，设置入口在“我的”

记一笔悬浮按钮在右下角，不遮挡列表，设置页隐藏

明细能按日期分组、筛选、左滑删除、点击编辑

统计支持周 / 月 / 年，饼图只统计支出

我的能管理账户、分类、标签、商家、预算

设置能改主题、货币、记账偏好，不包含通知、隐私与安全、数据备份

JSON 能完整导出，导入有预览、模式选择、冲突处理

导入失败能回滚，原数据不丢

导入后明细、统计、预算、账户同步刷新

删除分类后，关联账单回退到无类型

做安卓软件，UI 设计参考苹果的，同时加入液态玻璃效果，液态玻璃规范严格基于 liquid\_glass\_widgets 官方示例页面，不要使用其他玻璃效果库

所有做完，自己跑通所有测试，交付一个apk正式签名包，可以直接下载使用

九、给 AI 的开发指令
请根据以上需求，生成完整的 Flutter 项目代码。要求：

使用 Flutter 3.x，状态管理使用 Provider，本地数据库使用 sqflite，图表使用 fl\_chart，日期格式化使用 intl，液态玻璃效果使用 liquid\_glass\_widgets。

所有页面使用 Cupertino 风格组件，配合 liquid\_glass\_widgets 提供的玻璃脚手架、玻璃卡片、玻璃按钮、玻璃底部导航、玻璃悬浮按钮等。

严格实现三个 Tab 和所有独立页面的功能与交互。

数据模型、JSON 导入导出、冲突处理、事务回滚等逻辑必须完整。

代码结构清晰，按 models、providers、db、screens、widgets、utils 分层。

确保验收标准全部满足。

不要输出与需求无关的代码或解释，直接生成可运行的项目代码。

